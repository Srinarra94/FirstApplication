@model IEnumerable<FirstApp.Models.StudentMarks>

@{
    ViewData["Title"] = "Index";
}

@* < *@h1>Index</h1>
<a asp-controller="Students" asp-action="Index" class="btn btn-secondary">Go to Students</a>
<div style="float:left">
  <a class="btn btn-primary" asp-action="Create"><i class="fas fa-plus "></i>CreateRecord </a>
</div>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalMarks)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Grade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalMarks)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit Record</a> |
                <a asp-action="Details" asp-route-id="@item.Id">marks Record</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete Record</a>
            </td>
        </tr>
}
    </tbody>
</table>
